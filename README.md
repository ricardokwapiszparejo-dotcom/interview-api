# interview-api

REST API skeleton — Express 5 + TypeScript + Vitest.

```bash
pnpm dev    # start with hot reload on :3000
pnpm test       # run tests
```

Example resource: `src/tasks/` (entity → repository interface → in-memory repo → service → router).
